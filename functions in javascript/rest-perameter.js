function temperd(a,b,...c){//this three dots is rest perameter a due to a takes one value b takes one value but 3 takes all remaining value dua to rest perameter
    console.log(`a is ${a}`);
    console.log(`b is ${b}`);
    console.log(`c is ${c}`);
}
temperd(1,2,3,45,6,7,8,);
