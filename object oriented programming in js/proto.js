//  proto inherit the property in a object 
//  we need to specify a proto for our object to inherit the property
//  we can specify the proto by using new keyword to create the object if we are creating the object using a function
//  we can also specify the proto by using command object.create() and inside the perantahesis we need to specify the __proto__ or [[prototype]] here
